{
  "name": "sem7",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack project - run npm run dev to start all services",
  "scripts": {
    "db": "docker compose up -d",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "npm run db && wait-on tcp:localhost:1433 && concurrently \"npm run backend\" \"npm run frontend\"",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
